<section>
    <table class="table table-responsive">
        <thead>
            <tr>
                <th >Titre</th>
                <th >Image</th>
                <th >Publié le</th>
               
                <th>
            </tr>
        </thead>
        {{#each dbArticle}}
        <tbody>
            <tr>
                <td style="width: 25%">{{ title }}</td>
                <td style="width: 25%"><img src="{{ image }}" class="card-img-top" alt="..." style="width: 50%;"></td>
                <td style="width: 25%">{{ moment createDate format="DD-MM-YYYY"}}</td>

                <td style="width: 25%">
                    <a class="btn btn-info" href="/ArticleSingle/{{_id}}" role="button">Voir l'article</a>
                   
                </td>
            </tr>
        </tbody>
        {{/each}}
    </table>
</section>